{{#if scores.model.length}}
  <div class="blue-round player-scores">
    <h3>Your Scores</h3>
    <table>
      <thead>
        <th>Snippet</th>
        <th>WPM</th>
        <th>Accuracy</th>
      </thead>
      <tbody>
        {{#each scores.model as |score|}}
          <tr>
            <td>{{#link-to 'snippet.index' score.snippetId}}{{score.snippetId}}{{/link-to}}</td>
            <td>{{score.wpm}}</td>
            <td>{{score.accuracy}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{/if}}